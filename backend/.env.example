# Example Backend Environment File
# Copy this to .env and fill in the values (ask team lead)

# Supabase Configuration
SUPABASE_URL=https://ykfnyxlcpmfmmhwxgtyf.supabase.co
SUPABASE_SERVICE_ROLE_KEY=your_service_role_key_here
SUPABASE_ANON_KEY=your_anon_key_here

# RunPod GPU Instance (shared)
# Choose ONE of these SSH authentication methods:

# Option A: Base64-encoded key (recommended - easier to share)
SSH_HOST=216.81.248.127
SSH_PORT=10749
SSH_USER=root
SSH_KEY_BASE64=your_base64_encoded_key_here

# Option B: SSH key file path (alternative)
# SSH_KEY_FILE=.ssh/provision_team_key

# Remote Paths on GPU server
REMOTE_BASE_DIR=/workspace/provision/data
REMOTE_VIDEO_DIR=/workspace/provision/data/videos
REMOTE_RESULTS_DIR=/workspace/provision/data/results

# Model Server (on RunPod)
MODEL_SERVER_HOST=localhost
MODEL_SERVER_PORT=8765

# RunPod dashboard wrapper (optional, for /api/analytics/{session_id}/runpod-dashboard)
RUNPOD_DASHBOARD_REPO_DIR=/workspace/UpliftingTableTennis
RUNPOD_DASHBOARD_WRAPPER_PATH=/workspace/UpliftingTableTennis/provision_dashboard_pipeline.py
RUNPOD_DASHBOARD_PYTHON=python3
RUNPOD_DASHBOARD_BUCKET=provision-videos
RUNPOD_DASHBOARD_OUTPUT_SOURCE_DIR=/workspace/UpliftingTableTennis/output
# Optional: command executed in RUNPOD_DASHBOARD_REPO_DIR; supports {input}, {output_dir}, {session_id}
# RUNPOD_DASHBOARD_INNER_COMMAND="python3 run_inference_full_video.py --video {input} --max_frames 900"
RUNPOD_DASHBOARD_TIMEOUT=7200

# API Configuration
CORS_ORIGINS=http://localhost:3000,http://localhost:3001
API_HOST=0.0.0.0
API_PORT=8000
DEBUG=true

# AI APIs (optional)
OPENAI_API_KEY=
ANTHROPIC_API_KEY=
